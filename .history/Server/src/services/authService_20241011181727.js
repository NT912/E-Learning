export const authService = {
  signup: (userData, callback) => {
    // Thực hiện logic đăng ký
    // Gọi callback(err, result) sau khi xử lý xong
  },

  login: (credentials, callback) => {
    // Thực hiện logic đăng nhập
    // Gọi callback(err, token) sau khi xử lý xong
  },
};
