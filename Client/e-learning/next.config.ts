import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.alphacoders.com", "example.com"], // Thêm các domain mà bạn sử dụng
  },
};

export default nextConfig;
